-- Render Markdown plugin configuration
-- Provides enhanced markdown rendering directly in Neovim

require("render-markdown").setup({
  -- Using default settings for minimal configuration
  -- The plugin will automatically:
  -- - Render in normal, command, and terminal modes
  -- - Show enhanced headings with icons and colors
  -- - Style code blocks with language indicators
  -- - Format tables, lists, and checkboxes
  -- - Render block quotes and inline formatting
})
